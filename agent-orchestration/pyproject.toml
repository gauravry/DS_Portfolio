[project]
name = "agent-orchestration"
version = "0.1.0"
description = "Multi-agent trading orchestration scaffold"
requires-python = ">=3.11"

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["services", "libs"]
